# RNN-vs-LSTM-BBC-News-Classification
A Deep Learning project comparing RNN and LSTM models for text classification on the BBC News dataset. The project evaluates performance metrics such as accuracy and loss to highlight differences between sequential models.
